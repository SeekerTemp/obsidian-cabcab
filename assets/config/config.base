views:
  - type: table
    name: Configuration records
    filters:
      and:
        - file.inFolder("data/config")
        - file.name != "config.base"
    order:
      - file.name
      - file.path
    sort:
      - property: file.name
        direction: ASC
